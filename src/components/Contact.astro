<div class="container" id="Contacto">
  <h1>Hablemos de tu próximo gran proyecto</h1>
  <p>
    Listo para transformar tu idea en una realidad digital? Contáctanos y
    descubre cómo The Master SAS puede ser tu socio estratégico.
  </p>
  <form class="contact-form">
    <div class="input-group">
      <label for="name">Nombre Completo</label>
      <input type="text" id="name" required />
    </div>

    <div class="input-group">
      <label for="email">Correo Electrónico</label>
      <input type="email" id="email" required />
    </div>

    <div class="input-group">
      <label for="company">Empresa</label>
      <input type="text" id="company" required />
    </div>

    <div class="input-group">
      <label for="interest">Interés principal</label>
      <select id="interst" required>
        <option value="" disabled selected>Selecciona una opción</option>
        <option value="software">Software a Medida</option>
        <option value="platforms">MelAppido</option>
        <option value="consulting">JustGo</option>
        <option value="other">Otra consulta</option>
      </select>
    </div>

    <div class="input-group">
      <label for="message">Mensaje</label>
      <textarea id="message"></textarea>
    </div>

    <button class="btn-form" type="submit">Enviar consulta</button>
  </form>
</div>

<style>
  .container {
    font-family: "Montserrat", sans-serif;
    width: 90%;
    max-width: 1200px;
    margin: 0 auto;
    text-align: center;
    padding: 1rem 1rem;
    /* background-color: lightcyan; */
  }

  .container h1 {
    font-size: 2.5rem;
    color: var(--azul-profundo);
    margin-bottom: 1rem;
    margin-top: 3rem;
  }

  .container p {
    font-size: 1.2rem;
    color: var(--azul-marino);
    margin-bottom: 2rem;
  }

  .contact-form {
    max-width: 600px;
    margin: 0 auto;
    background-color: var(--azul-marino);
    padding: 2rem;
    border-radius: 8px;
    box-shadow: 0 4px 10px rgba(0, 0, 0, 0.2);
    text-align: left;
  }

  .input-group {
    margin-bottom: 1.25rem;
  }

  .input-group label {
    display: block;
    margin-bottom: 0.5rem;
    font-weight: 600;
    color: var(--azul-palido);
  }

  .input-group input[type="text"],
  .input-group input[type="email"],
  .input-group select,
  .input-group textarea {
    width: 100%;
    padding: 0.75rem;
    border-radius: 5px;
    border: 1px solid var(--azul-marino);
    font-size: 1rem;
    color: var(--azul-palido);
    background-color: var(--grisaseo-claro);
    font-family: inherit;
  }

  .input-group input:focus,
  .input-group select:focus,
  .input-group textarea:focus {
    border-color: var(--lemon-tonic);
    outline: none;
    box-shadow: 0 0 5px 3px rgba(255, 249, 81, 0.25);
  }

  .btn-form {
    padding: 1rem 3rem;
    background-color: var(--lemon-tonic);
    border: 1px solid var(--azul-brillante);
    border-radius: 50px;
    cursor: pointer;
    margin-bottom: 1rem;
    margin-top: 1rem;
    box-shadow: 0 0 6px 2px rgba(0, 0, 0, 0.5);
    color: var(--azul-brillante);
    font-family: inherit;
    font-weight: 600;
    font-size: 1.1rem;
    transition: all 0.3s;
  }

  .btn-form:hover {
    background-color: var(--azul-brillante);
    border-color: var(--lemon-tonic);
    transform: translateY(-2px);
    color: var(--lemon-tonic);
    text-decoration: underline;
  }

  .btn-form:active {
    transform: translateY(0);
    box-shadow: 0 0 4px 1px rgba(0, 0, 0, 0.3);
  }

  textarea {
    resize: vertical;
    min-height: 120px;
  }

  @media (min-width: 768px) {
    .container {
      /* background-color: rgb(76, 245, 245); */
    }
    .container p {
      display: block;
      max-width: 800px;
      margin: 0 auto;
      margin-bottom: 2rem;
    }
  }
</style>
